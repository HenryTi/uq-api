import { Arr } from '../../il';
import { BEntity } from './entity';

export class BArr extends BEntity<Arr> {
    /*
    constructor(arr: Arr, context: Context) {
        super(arr, context);
        this.entity = arr;
    }

    build() {
        super.build();
    }*/
}
